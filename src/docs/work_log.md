# Work Log

## 2026-05-09

- Inspected copied folders: `accounts`, `core`, `dashboard`, `exam`, and `services`.
- Found copied references to missing old-project apps such as `course`, `student`, `subscription`, `analysis`, old staff permissions, `dashboard_logs`, `invoice`, and `view`.
- Removed old teacher, academic-year, division, profile-image, lesson, and video-quiz concepts from the active domain.
- Found the local Python command is `python3`; Django and DRF are installed, but Celery is not installed.
- Created the project implementation plan in `docs/project_plan.md`.
- Added `manage.py` because the copied backend had no Django entrypoint.
- Added `course` app with `Course` and `Unit`.
- Added `student` app and signal-based student profile creation for `accounts.User(user_type="student")`.
- Added `subscription` app with plans, selected plan courses, payment status, EasyPay invoice creation, and dynamic course-access checks.
- Removed the temporary shop compatibility app after confirming the project only needs plan subscriptions.
- Replaced old copied dashboard routes with focused staff endpoints for plans, courses, units, subscriptions, questions, and exams.
- Repaired `core.settings` and `core.urls` to include only apps that exist in this project.
- Added `core.permissions` and `core.pagination` compatibility shims used by copied exam/dashboard code.
- Removed copied video quiz behavior because the current website has no lesson/video layer.
- Changed student exam start access checks to use paid active plan subscriptions instead of the old course subscription flag.
- Added `Result.is_allowed_to_show_result` and `Result.is_allowed_to_show_answers` properties expected by copied serializers.
- Rebuilt exam migrations because the copied migrations depended on non-existent app migration numbers.
- Ran `python3 manage.py check`: passed.
- Ran `python3 manage.py makemigrations accounts student course subscription exam`: generated new migrations.
- Ran `python3 manage.py migrate`: applied all migrations to the local SQLite database.
- Ran `python3 manage.py test --verbosity 1`: 10 tests passed.
- Ran `python3 manage.py makemigrations --check --dry-run`: no model changes pending.
- Started the development server with `python3 manage.py runserver 127.0.0.1:8000`.
- Verified `/plans/` responds with `401` for anonymous requests, which confirms routing and authentication are active.
- Added `.gitignore` for generated Python cache, local SQLite DB, media, static, and `.env` files.
- Removed the shop/order cleanup path from `accounts`: no order serializer, no `/accounts/orders/`, no purchased-item archive/restore behavior.
- Moved EasyPay webhook handling from the deleted compatibility app into `subscription/webhooks.py`.
- Removed the compatibility app from `INSTALLED_APPS` and deleted the old shop package.
- Cleaned EasyPay/customer profile helpers so they reference plan subscriptions instead of old orders.
- Removed teacher-specific signin and the copied dashboard permissions integration.
- Deleted unused copied dashboard modules that referenced old-project invoice/view/logging utilities.
- Added focused dashboard question category, question/answer, and exam-question assignment endpoints.
- Added a dashboard workflow test covering question creation with answers and assignment to an exam.
- Ran `python3 manage.py check`: passed.
- Ran `python3 manage.py makemigrations --check --dry-run`: no changes pending.
- Ran `python3 manage.py test --verbosity 1`: 9 tests passed.
- Removed all domain-level teacher/year/division/lesson/video/profile-image code from accounts, course, student, dashboard, and exam.
- Converted exams and questions to relate to a course directly or a unit under a course.
- Regenerated migrations after the domain cleanup.
- Backed up the old local SQLite database to `db.sqlite3.before_domain_cleanup`, rebuilt `db.sqlite3` from the new migrations, and restarted the dev server.
- Added a dashboard regression test for course-level question and exam creation.
- Ran `python3 manage.py test --verbosity 1`: 10 tests passed.
- Ran `python3 manage.py makemigrations --check --dry-run`: no model changes pending.
- Added `dashboard/test_full_flow.py`, a full executable platform scenario covering dashboard setup, student OTP/signup/signin, plan subscription/payment confirmation, course access, exam submit/results, student bank/temp exams, student-created exams, webhook confirmation, device/ban/security controls, account update, password change, password reset, archive, and restore.
- Added `docs/full_testing_scenario.md` with the same data and endpoint flow in readable form.
- Fixed issues found by the full-flow scenario: nested unit creation serializer requirement, explicit student app config for profile signals, course-level admin-bank filtering, admin-bank permissions, and result-list annotation/property conflict.
- Ran `python3 manage.py test dashboard.test_full_flow --verbosity 2`: passed.
- Ran `python3 manage.py test --verbosity 1`: 11 tests passed.

## 2026-05-10

- Reviewed the copied `dashboard/old/exam.py` and `dashboard/old/views.py` dashboard exam code.
- Rebuilt `dashboard/serializers/exam/exam.py` to support the restored dashboard exam serializers against the current course/unit-only domain.
- Added cleaned dashboard exam endpoint implementations in `dashboard/views/exam.py` for exams, categories, questions, answers, essay scoring, exam banks, random exam banks, exam models, results, trials, student took/not-took reports, exam copying, question reordering, temp exam limits, similar questions, and admin question bank operations.
- Kept the current `/dashboard/exams/<exam_id>/questions/` add/list/remove contract active, and exposed the restored old-style question list/remove flow through `/dashboard/exams/<exam_id>/questions/list/` and `/dashboard/exams/<exam_id>/questions/<question_id>/remove/`.
- Added `dashboard/test_dashboard_exam_endpoints.py`, a regression flow for the restored dashboard exam endpoints.
- Ran `python3 manage.py check`: passed.
- Ran `python3 manage.py makemigrations --check --dry-run`: no model changes pending.
- Ran `python3 manage.py test dashboard.test_full_flow dashboard.test_dashboard_exam_endpoints --verbosity 1`: 2 tests passed.
- Ran `python3 manage.py test --verbosity 1`: 12 tests passed.
- Ran the full Postman collection through Newman with local Django at `127.0.0.1:8000` and a fake local BeOn SMS endpoint for OTP delivery.
- Fixed the Postman/Newman trailing-slash issue by converting collection URL objects to raw URL strings.
- Added `OTP_FIXED_CODE` support in `services.otp_service` so local OTP signup/password-reset flows can be tested deterministically without real SMS delivery.
- Fixed dashboard multipart MCQ creation so bracketed `answers[0][text]` form-data creates answers without leaking parser artifacts into the nested serializer.
- Fixed question creation from multipart form-data so omitted `is_active` keeps the model default `True` instead of becoming `False`.
- Fixed dashboard question-list endpoints to accept numeric string IDs from Postman JSON variables.
- Fixed temp/student-created exam result submission to return validation errors for invalid IDs instead of server errors.
- Corrected the device-auth regression test to send the real `Authorization` header used by SimpleJWT.
- Re-ran `docs/American_TEST_full_api.postman_collection.json`: 181 requests executed; zero unexpected `4xx`/`5xx` responses. Remaining `4xx` responses are intentional negative-path checks.
- Ran `python3 manage.py test --verbosity 1`: 12 tests passed.
- Added `docs/ER_MODEL_DESIGN.md`, a manager-facing ER design document with scoped business notes, high-level domain map, detailed Mermaid ER diagrams, access rules, and review notes.
- Added `docs/DASHBOARD_API_DOCUMENTATION.md` for the dashboard frontend developer and `docs/STUDENT_API_DOCUMENTATION.md` for the mobile/student developer, generated from the tested Postman collection and Newman response report with method, URL, body type, body examples, response examples, and validation/error notes.
- Moved the project files into a new repository-level `src/` folder while keeping `.git` and the root `.gitignore` at the repository root.
- Verified the relocated project with `python3 src/manage.py check`, `cd src && python3 manage.py check`, and `cd src && python3 manage.py test --verbosity 1`: 12 tests passed.
- Added `docs/American_TEST_full_api.postman_collection.json`, a full Postman v2.1 collection with dashboard admin flow, student flow, exam scenarios, result/reporting scenarios, security/device/archive endpoints, OTP/password reset endpoints, and EasyPay webhooks.
- Added collection variables, dummy scenario data, ordered request folders, token/ID capture scripts, and `form-data` bodies for endpoints that accept images or files.
- Added `docs/postman_collection_usage.md` with import/run notes and variable guidance.
- Validated the Postman collection with `python3 -m json.tool`.
- Moved non-exam dashboard views from `dashboard/views/__init__.py` into `dashboard/views/management.py`.
- Moved the remaining exam-question assignment/detail views into `dashboard/views/exam.py` so all dashboard exam endpoints live in one module.
- Reduced `dashboard/views/__init__.py` to a package marker and updated `dashboard/urls.py` to import `management_views` and `exam_views` explicitly.
- Ran `python3 manage.py check`: passed.
- Ran `python3 manage.py makemigrations --check --dry-run`: no model changes pending.
- Ran `python3 manage.py test dashboard.test_full_flow dashboard.test_dashboard_exam_endpoints --verbosity 1`: 2 tests passed.
- Ran `python3 manage.py test --verbosity 1`: 12 tests passed.
